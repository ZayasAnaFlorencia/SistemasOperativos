<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title || 'RBAC App' %></title>
  <link rel="stylesheet" href="/css/app.css" />
</head>
<body>
  <header class="topbar">
    <div class="brand">RBAC Demo</div>
    <nav>
      <% if (user) { %>
        <span class="user">👤 <%= user.nombreUsuario %> — <%= user.rol %></span>
        <form action="/logout" method="post">
          <button class="link" type="submit">Salir</button>
        </form>
      <% } %>
    </nav>
  </header>
  <main class="content">
    <%- body %>
  </main>
</body>
</html>
